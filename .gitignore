.venv/
__pycache__/
*.pyc
dist/
.DS_Store

# Local Claude Code session state (agent worktrees, etc.) — never shipped.
.claude/

# Local website design handoff + prototype (not shipped)
design_handoff_placeroot_website/
*.zip

# Local secrets — never commit
.env
.env.*

# wrangler local cache
.wrangler/
