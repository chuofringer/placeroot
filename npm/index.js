#!/usr/bin/env node
// Placeholder claiming the package name. The PlaceRoot MCP server is
// distributed on PyPI: run it with `uvx placeroot`. See https://placeroot.dev
console.error("PlaceRoot: run with `uvx placeroot` (Python). npm wrapper coming later.");
process.exit(1);
